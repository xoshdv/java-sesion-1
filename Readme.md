Ejercicio 1: Uso de @Getter y @Setter
1. Sin Lombok:
   o Crea una clase llamada Persona con los siguientes atributos:
   ▪ nombre (String).
   ▪ edad (int).
   o Implementa manualmente los métodos getNombre, setNombre,
   getEdad, setEdad.